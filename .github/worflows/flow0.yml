name :  first_flow

on : 
  push:
    branches: [main]

jobs:
  first_flow:
    runs-on: ubuntu-latest

    steps:
      - name: hello
        run : echo "Hello github actions -- first flow by is2ac"
